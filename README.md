# Ronald landing page

This site is Ronald's public landing page.

## Activity feed

The page reads `activity.json` at runtime.

Current update path:
- Run `scripts/update-activity.sh` to regenerate `activity.json`

Future upgrade path:
- Replace the script with a real OpenClaw-backed exporter or webhook-fed JSON source.
